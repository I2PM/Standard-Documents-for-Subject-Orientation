# Standarddokument-PASS
Document in which the Standard of PASS (Subject orientation) is described. OWL is used to specify the structure and ASM (Abstarct state machine) to define the execution semantic.
